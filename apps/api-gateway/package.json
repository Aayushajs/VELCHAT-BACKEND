{
  "name": "@velchat/api-gateway",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "jest",
    "start": "cross-env SERVICE_NAME=api-gateway HTTP_PORT=3000 node dist/main.js",
    "dev": "cross-env SERVICE_NAME=api-gateway HTTP_PORT=3000 tsx watch src/main.ts",
    "clean": "rimraf dist .turbo coverage"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15",
    "@nestjs/platform-express": "^10.4.15",
    "@velchat/config": "workspace:*",
    "@velchat/event-bus": "workspace:*",
    "@velchat/shared-types": "workspace:*",
    "@velchat/shared-utils": "workspace:*",
    "pino": "^9.6.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  }
}
