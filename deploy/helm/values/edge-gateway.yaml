# @velchat/edge-gateway — Kubernetes values (AWS EKS / Azure AKS / OKE).
# Same image the compose deployment runs; only the orchestrator differs.
name: edge-gateway
image:
  repository: ghcr.io/aayushajs/velchat-edge-gateway
  tag: latest
service:
  port: 3001
env:
  SERVICE_NAME: edge-gateway
  HTTP_PORT: '3001'
