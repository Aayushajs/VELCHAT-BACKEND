{
  "name": "@velchat/cache",
  "version": "0.1.0",
  "private": true,
  "description": "Valkey/Redis cache client (shared) — connection + health as a ManagedResource.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "clean": "rimraf dist .turbo coverage"
  },
  "dependencies": {
    "@velchat/shared-utils": "workspace:*",
    "ioredis": "^5.4.2"
  }
}
