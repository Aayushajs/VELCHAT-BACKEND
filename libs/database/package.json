{
  "name": "@velchat/database",
  "version": "0.1.0",
  "private": true,
  "description": "Shared data-store clients: Postgres (with §G6 RLS guardrail) + MongoDB. Connection + health only — each service still owns its own schema (§A10).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "jest",
    "clean": "rimraf dist .turbo coverage"
  },
  "dependencies": {
    "@velchat/common": "workspace:*",
    "drizzle-orm": "^0.38.3",
    "mongoose": "^8.9.5",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10"
  }
}
