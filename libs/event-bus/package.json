{
  "name": "@velchat/event-bus",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "jest",
    "clean": "rimraf dist .turbo coverage"
  },
  "dependencies": {
    "@velchat/config": "workspace:*",
    "@velchat/shared-utils": "workspace:*",
    "ioredis": "^5.4.2",
    "kafkajs": "^2.2.4",
    "pino": "^9.6.0"
  }
}
