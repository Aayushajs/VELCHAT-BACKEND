/** call feature — owns no infrastructure; the composition root injects it. */
export { CallsModule } from './calls/calls.module';
export { ScreenControlModule } from './screen-control/screen-control.module';
