/** notification feature — owns no infrastructure; the composition root injects it. */
export { CampaignModule } from './campaigns/campaign.module';
export { NotificationModule } from './notify/notification.module';
