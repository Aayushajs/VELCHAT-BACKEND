{
  "name": "@velchat/push",
  "version": "0.2.2",
  "private": true,
  "description": "Push notifications (shared) — Web Push (VAPID) + FCM port/adapters. §B10: E2EE payloads carry no content.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "clean": "rimraf dist .turbo coverage"
  },
  "dependencies": {
    "@velchat/config": "workspace:*",
    "@velchat/common": "workspace:*",
    "jsonwebtoken": "^9.0.2",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.7",
    "@types/web-push": "^3.6.4"
  }
}
